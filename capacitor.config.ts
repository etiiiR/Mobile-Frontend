import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'CDL1 Sensor Activities',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
